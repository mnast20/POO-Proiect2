package elf;

import child.Child;

public class WhiteElfStrategy implements ElfStrategy {
    /**
     * Method executing the white elf strategy, which means nothing
     */
    public void execute(final Child child) {
        // does nothing
    }
}
